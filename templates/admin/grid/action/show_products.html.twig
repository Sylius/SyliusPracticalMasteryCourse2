{% set path = path(options.link.route, options.link.parameters) %}

<a href="{{ path }}" class="btn btn-icon" data-bs-toggle="tooltip" data-bs-title="{{ action.label|trans }}" target="_blank">
    {{ ux_icon('tabler:eye') }}
</a>
