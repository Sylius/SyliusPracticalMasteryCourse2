<form action="{{ path('sylius_admin_brand_bulk_export') }}" method="post" data-bulk-export="index">
    <button type="submit" class="btn btn-ghost-primary" data-check-all-action="index">
        {{ ux_icon('tabler:file-export') }}
        Export
    </button>
</form>
